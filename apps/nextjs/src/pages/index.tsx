import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div>
      This app is a scheduler. It lets employees request tiem off and lets them
      know what days the organization is closed. You also need an account to use
      this app
    </div>
  );
};

export default Home;
